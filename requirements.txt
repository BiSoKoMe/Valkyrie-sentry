psutil>=5.9.0
dnslib>=0.9.24
winotify>=1.1.0

#.\build.ps1                    # → dist\ValkyrieSetup.exe  (full installer)
#\build.ps1 -FreezeOnly -Run   # → builds the app and launches it, NO installer
# $.\run-dev.ps1                  # → runs the app straight from source, seconds