# Valkyrie modular package dependencies
dnspython>=2.4.0
psutil>=5.9.0
rich>=13.0.0
pyyaml>=6.0

# Web dashboard (used by start_all.bat / start_valkyrie.bat daily flow)
fastapi>=0.110.0
uvicorn>=0.29.0
# WebSocket implementation uvicorn needs to serve the dashboard's live /ws feed.
# WITHOUT this, uvicorn answers the /ws upgrade with HTTP 404, the dashboard's
# only live channel is dead, and the page shows its initial snapshot forever
# (looks like "the dashboard never updates"). Equivalent: install uvicorn[standard].
websockets>=12.0

# Optional - domain age scoring (WHOIS)
# python-whois>=0.8.0

# Optional - AI-assisted EDR investigation (opt-in; sends incident details to
# the Claude API — off by default). Without it, the offline analyst is used.
# anthropic>=0.40.0

# Optional - signing/verifying fleet policy + remote-response commands
# (Ed25519). Only needed on the control plane and for signed remote response.
# cryptography>=41.0.0

# Optional - TLS inspection (HTTPS interception; enable with --tls)
# mitmproxy>=10.0.0

# Optional - page content cleaner (HTML parsing in tls_addon.py)
# lxml>=5.0.0
